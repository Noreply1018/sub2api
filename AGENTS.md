- 使用中文回复用户，并使用中文编写文档。
- 当仓库改动来自 Codex 自己时，完成验证后应自觉执行 git 提交；不要提交用户已有或无关改动。
- 分支习惯：`local-custom` 作为本 fork 的长期本地开发基线，不直接在其上做具体功能改动；开始二开任务前，应先从 `local-custom` 切出短分支，例如 `feat/<topic>`、`fix/<topic>` 或 `docs/<topic>`，完成验证和提交后再按需合回 `local-custom`。
